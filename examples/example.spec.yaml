openapi: 3.0.0
info:
  title: API Name
  version: 1.0.0
paths:
  /users:
    get:
      summary: what it does
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
  /users/{id}:
    get:
      summary: get a user by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
    delete:
      summary: delete a user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string